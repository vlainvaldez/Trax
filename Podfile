# Uncomment the next line to define a global platform for your project
platform :ios, '11.0'

target 'Trax' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  # Pods for Trax
  pod 'SnapKit', '~> 5.0.0'
  pod 'Moya', '~> 13.0'
  pod 'Kingfisher'
  pod 'PKHUD', '~> 5.0'

  target 'TraxTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TraxUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
